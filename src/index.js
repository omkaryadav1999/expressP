const express = require("express");
const app = express();

// // navigation menu in express


// app.get("/", (req, resp) => {
//     resp.send("hello this is Home page")
// })

// app.get("/about", (req, resp) => {
//     resp.send("this is the about page")
// })

// app.get("/contact", (res, resp) => {
//     resp.send("this is the contact page")
// })


// app.listen(5000);


// send the json data;

// app.get("/data", (req, resp) => {
//     //express.js behind the scean convert this data into the json ;
//     resp.send([
//         { id: 1, name: "omkar", age: 23 },
//         { id: 2, name: "ashwarya", age: 24 }
//     ])
// });

// app.listen(5000)